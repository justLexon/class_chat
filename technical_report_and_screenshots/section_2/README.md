# Section 2 Screenshot Notes

This screenshot shows the advanced client behavior during a persistent session.

What it shows:

- the client staying connected after the first message
- multiple messages exchanged in one session
- the server still responding while the connection remains open

Why it matters:

This demonstrates that the client no longer closes after a single request and can continue interacting with the server during the same session.
